<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/home">Bus Ticket</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <!-- <li class="nav-item active">
          <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a>
        </li> -->
      <% if( isAuthentication ){%>
      <form class="form-inline my-2 my-lg-0" action="/user/dashboard" method="GET">
        <input type="hidden" name="_csrf" value="<%= csrfTocken %>" />
        <button class="btn btn-outline-primary mx-2 my-2 my-sm-0" type="submit">Dashboard</button>
      </form>
      <form class="form-inline my-2 my-lg-0" action="/user/logout" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfTocken %>" />
        <button class="btn btn-outline-danger my-2 my-sm-0" type="submit">LogOut</button>
      </form>
      <% } else { %>

      <li class="nav-item">
        <a class="nav-link" href="/user/login">Login</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user/signup">Signup</a>
      </li>
      <% } %>
    </ul>
  </div>
</nav>